<div class="container px-5 px-sm-0">
  <div class="row">
   <div class="col-md-3">
    <h1>PanImage#index</h1>
    <p>Find me in app/views/pan_image/index.html.erb</p>
    <div class="col-md-8 offset-md-1">
  
      <h2>Books</h2>
      <table class="table table-hover table-inverse">
      
      <thead>
      <tr>
        <th></th>
        <th>Title</th>
        <th>Opinion</th>
        <th colspan="3"></th>
      </tr>
      </thead>
      <tbody>
            
            </td>
            <% @pan_images.each do |pan_image| %>
              <tr>
                <td>
                <%= link_to pan_image_path(pan_image.id) do %>
                  <%= image_tag pan_image.get_image %>
                <% end %>
            </td>
            <td>ショップ名：<%= pan_image.shop_name %>
            </td>
            <td>説明：<%= pan_image.caption %>
            </td>
            <td>ユーザーネーム：<%= pan_image.user.name %></td>
             <td><%= link_to "#{pan_image.pan_comments.count} コメント", pan_image_path(pan_image.id) %></td>
          </div>
        <% end %>
        </tbody>
        </table>
        </div>
        </div>
        </div>